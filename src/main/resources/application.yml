server:
  port: 8080

spring:
  profiles:
    include: secret

  mail:
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          timeout: 5000

  jpa:
    open-in-view: false  # 👈 이 줄을 추가해 주세요!
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect